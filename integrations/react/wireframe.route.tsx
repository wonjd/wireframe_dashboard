/**
 * React (Vite / CRA / react-router) — Router에 마운트
 *
 * 복사 위치 (detect 결과 참고):
 *   src/routes/wireframe.tsx   — react-router
 *   src/pages/Wireframe.tsx    — 기타 SPA
 *
 * 필요:
 *   - @wireframe-studio/react workspace 의존성
 *   - dev server / reverse proxy 에서 /wireframe/api → wireframe API
 *   - Router에 /wireframe/* 경로 추가
 */
export { default } from "@wireframe-studio/react";
