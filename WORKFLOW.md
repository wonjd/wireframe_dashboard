# 워크플로우

사람 관점. 에이전트 관점은 [AGENTS.md](./AGENTS.md).

```
[요청자]  PRD 붙여넣기            ← 사람 접점 ①
    │
    │  ════ 이하 무인 ════
    │  정규화 → 구조 판정 → 화면 설계 → 화면 생성(병렬)
    ▼
[요청자]  화면을 본다
    │
    ├─ 고칠 데가 있다 → 그 화면에 대고 지시   ← 사람 접점 ②
    │      그 화면만 다시 그려진다. 나머지는 그대로
    │      ↑───────────── 마음에 들 때까지 ─────────────┘
    │
    └─ 됐다 → 확정                          ← 사람 접점 ③
           │
           ▼
[개발자]  화면 + 지시 로그 + 구조 판정
```

사람이 닿는 곳은 셋뿐이다. 입력 · 반복 · 확정.

---

## 요청자

**입력.** 양식 없다. 이메일 본문을 그대로 붙여넣어도 된다. 정보가 빠져도
멈추지 않는다 — 에이전트가 가정을 세워 채우고, 그 가정을 화면 옆에 표시한다.

**반복.** 화면에서 고칠 요소를 지목하고 말한다. 화면을 말로 서술할 필요는 없다.

| 요청 | 걸리는 시간 |
| --- | --- |
| "이거 크게" · 색 · 여백 | 즉시 |
| 요소 추가 · 문구 수정 | 3–5초 |
| 화면 구조 변경 | 20–30초 |
| PRD 자체가 바뀜 | 1–2분 |

**마음에 든 화면은 잠근다.** 잠근 화면은 전체 재생성에도 안 바뀐다.

**확정.** 그 시점 화면과 지시 로그가 고정되고 개발자에게 링크가 간다.
이후 다시 만지면 개발자 쪽에 "확정본과 달라짐"이 뜬다.

---

## 개발자

URL 하나 받는다. `/wireFrame/{프로젝트}/{feature}`

| 탭 | 보는 것 |
| --- | --- |
| PRD | 요청 원문 · 정규화된 PRD · 에이전트가 세운 가정 |
| 화면 | 와이어프레임. 개발 정보는 접혀 있다 |

**지시 로그가 명세다.** 요청자가 친 지시들이 곧 의도다.
로그에 있는 건 의도적인 결정, **로그에 없는 건 신경 안 쓴 것 = 개발자 재량.**
이 한 줄 규칙이 "이 버튼 색은 정한 건가요" 왕복을 없앤다.

**구조 판정을 먼저 봐라.** 화면은 요청자가 그린 "이런 걸 원한다"이지 명세가
아니다. 필드가 실제와 안 맞으면 맞추면 된다 — 다만 카디널리티가 틀렸으면
그건 요청자가 못 잡는 오류라 짚어줘야 한다.

---

## 팀 DB

이 레포는 DB를 갖지 않는다. 팀 MySQL이 필요하면 `wireframe_issue.sql`을
클론 프로젝트 DB에 한 번만 실행한다.
