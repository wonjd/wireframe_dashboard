# 워크플로우

사람 관점. 에이전트 관점은 [AGENTS.md](./AGENTS.md).

```
[요청자]  PRD 붙여넣기                         ← 사람 접점 ①
    │
    │  ════ PRD 확정·보완 (채팅) ════
    │  모호한 결정만 질문 → 답변 → **무엇으로 확정할지 제시** → 승인 → ## 확인된 결정 → ready
    │  PRD에 근거가 있는데 답이 이를 부정하면 근거를 인용해 1회 되묻는다
    │  ════ ready 이후 ════
    │  화면 양식만 질문 (페이지/모달/표/단계) → phase=layout
    ▼
[요청자]  와이어프레임 생성 (ready + 양식 확정 후)
    │  이후 PRD가 바뀌면 기능명세서·유저플로우·화면이 함께 다시 만들어진다
    │  ════ 이하 무인 ════
    │  domain → plan → render (triple: PRD + JSON + live DB)
    ▼
[요청자]  화면을 본다  (상단 플로우 버튼 = HTML 1장씩 · 가운데 비율 맞춤)
    │
    ├─ 고칠 데가 있다 → 그 화면에 대고 지시   ← 사람 접점 ②
    │      그 화면만 다시 그려진다. 나머지는 그대로
    │      ↑───────────── 마음에 들 때까지 ─────────────┘
    │
    └─ 됐다 → 확정                          ← 사람 접점 ③
           │
           ▼
[개발자]  화면 + 지시 로그 + 구조 판정
```

사람이 닿는 곳은 셋뿐이다. 입력 · 반복 · 확정.

---

## 요청자

**입력.** 양식 없다. 이메일 본문을 그대로 붙여넣어도 된다.  
PRD 채팅이 **쉬운 말**로 빈칸을 묻는다 (개발 용어·컬럼명 없음).  
**화면 양식**(전체 페이지 / 팝업·모달 / 목록 표 / 단계별)은 애매한 부분이
다 확정되고 **PRD 승인(ready) 뒤에만** 묻는다.

**보기.** `/wireframes`에서 목록을 고르면, 사이드바 없이 **플로우 버튼 + 해당 HTML만**
보인다. HTML은 뷰포트 **가운데·비율 맞춤**, 스크롤 없음. 설명 문구 없이 UI·동작만.

**반복.** 화면에서 고칠 요소를 지목하고 말한다. CLI `render --artifact … --instruction …`.

| 요청 | 걸리는 시간 |
| --- | --- |
| "이거 크게" · 색 · 여백 | 즉시~수 초 |
| 요소 추가 · 문구 수정 | 수 초 |
| 화면 구조 변경 | 수십 초 |
| PRD 자체가 바뀜 | 1–2분 (+ ready 재게이트) |

**마음에 든 화면은 잠근다.** 잠근 화면은 전체 재생성에도 안 바뀐다.

**확정.** 그 시점 화면과 지시 로그가 고정되고 개발자에게 링크가 간다.

---

## 개발자

URL 하나 받는다. `/wireframes/{projectNo}/{run}/screens/{id}`

| 탭 | 보는 것 |
| --- | --- |
| PRD | 요청 원문 · 확인된 결정 · 가정 |
| 화면 | 와이어프레임 HTML (플로우별 1장) |

**지시 로그가 명세다.** 요청자가 친 지시들이 곧 의도다.
로그에 있는 건 의도적인 결정, **로그에 없는 건 신경 안 쓴 것 = 개발자 재량.**

**구조 판정을 먼저 봐라.** `spec/domain.json`의 judgements · uiPattern.

---

## 환경

라이브 DB·SSH는 `.env`만 (`SSH_*` / `DB_*`). SELECT 전용.  
OpenAI는 `OPENAI_API_KEY`. 자세한 키: `.env.example`.
