# Wireframe Studio — API 키 불필요.
# 생성은 wf generate → prompt.txt → Claude (구독) → wireFrame/issue/{id}.html
#
# 이 레포는 DB를 갖지 않는다. 클론 프로젝트 MySQL만 사용.
# DATABASE_URL=mysql://user:pass@127.0.0.1:3306/your_db
# 없으면 뷰어는 wireFrame/ 파일만 본다.
