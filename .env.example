# Cursor API 키 (crsr_...). 서버 전용 — NEXT_PUBLIC_ 접두사 금지.
# cursor.com/dashboard → API Keys 에서 발급.
CURSOR_API_KEY=

# 기본값 https://api.cursor.com. 바꿀 일 없으면 비워 둘 것.
# (api2.cursor.sh 를 넣으면 동작하지 않는다.)
# CURSOR_API_BASE_URL=
